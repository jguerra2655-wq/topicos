docker run -d \
  --name multillidae \
  --network multillidae-net \
  -p 80:80 \
  -e DB_HOST=multillidae-mysql \
  -e DB_NAME=mutillidae \
  -e DB_USER=mutillidae \
  -e DB_PASSWORD=mutillidae \
  citizenstig/nowasp


# Eliminar contenedor actual
docker stop multillidae && docker rm multillidae

# Ejecutar con variables de entorno para configuración básica
docker run -d \
  -p 80:80 \
  --name multillidae \
  -e MYSQL_HOST=127.0.0.1 \
  -e MYSQL_USER=root \
  -e MYSQL_PASSWORD=mutillidae \
  citizenstig/nowasp